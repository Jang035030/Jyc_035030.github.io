补充章节二 Kali系统里面进行渗透测试（Linux系统） 
1 .Sqlmap说白了就是判断网站是否存在sql注入漏洞的、并利用漏洞(查看数据库信息 如账号 密码等)的工具
--
这里补充一下Sqlmap的知识
> 这是sqlmap一些简单的命令

Kali里面完整的命令就是：
sudo sqlmap -u “要测试的网站” --random-agent  
--cookie PHPSESSID=b26b66ck03s96eimlthf5llv66 --batch -D --dbs
（其实可以把网址地址和Cookie值，发给AI，让ai写命令）
------
测试前，需要把网站，先在kali虚拟机的 浏览器里面成功登录运行，浏览器页面按F12——Network——点击对应的网站地址——Cookies——获取钥匙(如图所示)

> 利用sqlmap命令，获取数据库信息（账号、密码）

这时候可以，ssh jehad@192.168.x.xx，输入密码fool，利用该账号，登录到了网站系统里面去，相当于入侵成功
这相当于初步登录了该网站的账号，接下来就是要向内部渗透，获取更多的信息

2 .Linux系统本地收集信息
> 当我们有幸获取了SSH账号，并且成功渗透到该网站/受害者电脑中后，接下来要做的第一步就是信息收集（搜集受害者电脑/被入侵网站的 相关信息，比如：）
这时候kali虚拟机（Linux系统）充当的就是攻击者的电脑/工具，被入侵的电脑可能是其他系统（比如：windows、Ubuntu乌班图系统）
> 接下来将 Linux入侵后的信息收集的一些命令（跟上面的2 .那个不一样，那个是入侵前，这个是入侵后）

这些命令和入侵前信息收集的命令基本类似
登录SSH账号后，在Linux/Kali中执行这些命令，可以获取受害者电脑的信息
> Apach 、Mysql、Nginx、、FTP这四个分别代表什么？
Nginx / Apache（网页服务器） = 门卫/接待员。它们负责接待访客（浏览器）的请求，并把网页内容发给访客。Nginx 像一位高效的前台，擅长同时处理大量访客；Apache 像一位功能强大的管家，配置灵活但人多时可能慢一些。
MySQL（数据库） = 档案库/仓库。它负责存储网站的所有数据，比如用户注册信息、文章内容、商品列表。你看到的动态内容（比如用户名、价格）都是从这里面调取出来的。
FTP（文件传输协议） = 搬运货物的后门。它不是网站运行必须的部分，而是一个上传工具。当你需要把网站代码、图片等文件从自己的电脑传到服务器上时，就需要通过FTP这个“搬运通道”。
真正搭建网站的时候FTP一般不需要

3 .操作系统Linux计划任务权限提升（提权）
> 我们通过执行命令 获取到受害者一大堆信息后，接下来就是要提权（就是拿到最高权限root，然后才能完整地控制受害者电脑）
> LINUX提权的方式有很多【SUID】【sudo滥用】【计划任务】【自启动】【内核提权】
【第三方软件提权】【环境变量提权】【通配符提权】等等方式很多，
我们今天来学习"计划任务提权"
真正的渗透测试实战中，需要把每一种方法都试一试，并不是每个方法都能成功

> 计划任务，顾名思义，就是按照规定的计划时间，执行固定的程序，
当我们需要在服务器上定时执行一些重复性的事件时使用的，可以通过计划任务程序来运行准备好的脚本、批处理文件夹、程序或命令，在某个特定的时间运行。
> 计划任务的文件夹在 /etc/cr on*——打开cat /etc/cron*——cat /etc/crontab 可以查看计划任务，如下图 有多条任务正在运行
之前，做恢复系统的时候，就已经用到过计划任务了

> 计划任务提权 方法一：改计划任务
有些管理员，安全防护做的不到位，导致普通用户也能修改计划任务的内容（甚至可以添加计划任务 或删除计划任务），这时候我们可以尝试 修改计划任务来提权
比如说，把root service apache2 start && service mysql start 改成一个木马，
---
执行以下命令来打开编辑器，来修改计划任务
vim /etc/cron
vim /etc/crontab
看看能不能修改和保存，能的话就破坏掉命令 或改成一个木马；不能的话就换另一种方法
> 计划提权任务 方法二：找到计划任务的记录，然后观察是否存存在高危险的命令和运行
程序，看看是否能提权
比如这个计划任务 * * * * * losy cd/opt/web && php -S localhost:9999
经过一系列操作，拿到用户losy的权限（这里的操作有点复杂，具体可以让AI辅助执行）

<这里非常麻烦，需要反复操作，执行一些复杂而陌生的命令，以及复杂陌生的操作；
我这里第一遍听不懂>

4 .Kali美杜莎工具账号密码破解
> 通过上面的步骤，我们已经拿到了两个用户的权限，jehad和losy，通过查看/etc/pass wd
文件发现还有一个用户是lama，我们还没拿到权限
> lama用美杜莎工具（kali里面已经自带安装了，我们不需要手动安装），来暴力破解，获取其密码（账号已知，lama）
命令可以问AI，密码字典可以kali浏览器baidu.com里面下载，放到kali虚拟机桌面上
medusa -M ssh -h 192.168.41.136 -u lama -P /home/kali/Desktop/top100_ssh.txt -t 10 -F
出现[SUCCESS]，说明密码破出来了
这个时候就可以登录用户lama权限


到这里，这三个账号jehad、losy、lama已经破解出来了（拿到这三个账号的权限了），接下来就剩下一个最高权限root
Jehead: 利用sqlmap查看数据库，获取到密码
Losy: 提权，查找记录，获取到密码
Lama: 美杜莎暴力破解，获取到密码
这些用户ID都是1000出头，都是低权限的用户，接下来要拿到root最高权限

5 .Sudo提权 获取root用户最高权限
> sudo介绍
SUDO是Linux系统管理指令，是允许系统管理员让普通用户执行一些或者全部的root命令的一个工具
在一些应用场景里面，为了方便运维人员以低权限帐号进行运维，往往会开启帐号的一些SUDO权限给运维帐号，从而减少root等登录频率

> 这里补充一下Linux系统常见的命令
（这里先记录一下常见命令，Linux操作系统作为一个大模块要单独学习）
面试官考Linux命令，核心就盯着文件管理、权限和状态排查。你不用全背，把这20个以内的高频命令及“考点”记牢就行：
----
cd / ls / pwd：切换、列表、显示当前路径。考点：ls -l 看权限，ls -a 看隐藏文件。
cp / mv / rm：复制、移动/重命名、删除。致命考点：rm -rf 慎用，面试必问“如何安全删除”（先ls确认再删）。
cat / less / tail：查看文件。考点：tail -f 实时看日志，绝对高频。
vim：编辑文件。考点：三种模式（命令/插入/底行），保存退出 :wq，强制退出 :q!。
chmod / chown：权限和归属。考点：chmod 755 的数字含义（属主7=rwx，属组5=rx，其他人5=rx）。
ps / top / netstat：看进程、看资源、看端口。考点：ps -ef | grep java 查进程，netstat -tlnp 查端口占用。
grep / find：文本搜索和文件查找。考点：管道符 | 组合使用，如 grep ERROR app.log。
df / du：看磁盘和文件大小。考点：df -h 看整体，du -sh * 看当前目录下各文件大小。
tar：打包压缩。考点：解压 tar -zxvf（z代表gzip，x解压，v显示，f文件）。
kill：终止进程。考点：kill -9 强制杀，kill -15 优雅终止（可被捕获清理资源）。

----
面试Linux操作系统，有3个必考题：
// 日志报错磁盘满？ 答：用 df -h 看分区，再用 du -sh * 找大文件，清空用 > filename。
// 端口被占用？     答：netstat -tlnp | grep 8080 查PID，然后 kill -9 PID。
// 权限不够？       答：加 sudo，或检查 ls -l 看属主，用 chmod 改权限。
----
最后记个“保命口诀”：增删改查用Tab补全，删前先看，改前先备份。

> 接下来将如何使用Sudo提权，获取root用户最高权限
先登录获取lama用户权限(这三个用户 哪个能行就用哪个，假设lama能行)——sudo -l 输入lama密码，查看——whoami 查看用户名(就是返回lama)——sudo whoami 返回root（不同的用户，不一定都返回root，要么返回root 要么命令执行失败或报错）——sudo id（id变成0，也就是最高权限）
总之，sudo xxxx ，就是在lama/... 用户权限下，以最高权限执行命令
（比如 sudo sqlmap -u） 

> sudo su root 就是切换到最高权限（root权限）


> 如果sudo whoami 没有返回root，而是命令执行失败（说明用户没有sudo权限，比如说losy用户没有sudo权限），那么现在该怎么提权呢？
答案：Python建立反弹SHELL连接
Python是一种高级的、动态类型的编程语言，它的设计注重代码的可读性和简洁性。
Python于1989年首次公开发布，至今已经成为了全球最受欢迎的编程语言之一。
----
在命令行输入python就进入到了编程页面
（这是Linux下的命令行终端（Shell）调用了Python解释器
Windows操作系统，配置完python环境变量后，在 CMD或 PowerShell 里输入 python，效果和 Linux 终端完全一样）。
----
Python的作用比较多，可以实现数学运算、网站编程、算法设计等，当然Python也可以去执行操作系统的命令，因为Python是由sudo命令启动的，所以也就是ROOT启动的Python，那么我们使用Python执行系统命令也就是ROOT的权限
也就是我们要说的，利用python来提权
接下来将实际操作（kali虚拟机）
su - losy → 完整切换到 losy 用户（环境变量也切换）——python3 进入python编辑器，里面可以敲各种各样的python语法代码来实现编程
sudo python3 → root权限启动


> python命令执行
在python3里面，分别执行一下两个命令
import os      //导入os库
os.system("命令")    //system(“命令”) 这个是命令执行函数


6 .Python建立反弹SHELL连接
上面讲到的，获取root权限必须用sudo ；那么我们不想用sudo，能不能直接获取root权限？
Python建立反弹SHELL连接
> Bash是许多Linux发行版的默认Shell，类似windows中的CMD命令行，使用Bash就可以启动一个Shell，从而执行操作系统的命令
我们可以通过Python执行Bash命令从而实现启动losy用户的Shell

> Shell 就是一个命令行解释器，webshell是网站木马

>具体操作：在 python3里面执行命令
import os      
os.system("bash")
打开之后，就直接变成root权限

或者是，在普通用户权限下，直接执行 sudo bash，不用python，也能从普通用户
权限—root权限

> sudo滥用
Sudo滥用是root乱给的权限，它不是漏洞，而是管理员在配置root的时候配置的
普通用户默认不能使用 sudo，管理员可以通过配置主动赋予这个权限
---
为什么必须给普通用户 sudo 权限？
因为 Linux 的设计原则是：
日常工作用普通用户（安全，防止误删系统文件）
需要管理操作时提权（通过 sudo 临时获得 root 权限）
----
如果不给 sudo 权限，普通用户连这些基础操作都做不了



7 .Shadow文件和特权账号
你作为攻击方，受害者（Linux操作系统）已经发现你对其电脑/服务器/...做出了入侵、篡改、窃取数据等操作，然后受害者就开始修复这些问题（漏洞bug等）。
那么攻击方下一次要入侵电脑，就变得十分困难。
这个时候，攻击方可以考虑留一个后门，下次入侵的时候，直接通过这个后门进入受害者电脑。
Linux操作系统，留后门的方法有许多，这里将：特权账号留后门
----
> 在Linux系统中,存在着两个特殊的文件 /etc/passwd 和 /etc/shadow 这两个文件中存储着用户名和加密后的密码.
在目前大多数Linux系统中,将加密后的用户密码存放在 /etc/shadow 中，但是 /etc/shadow 只能root用户查看.
那么有了root权限后，就能查看 用户密码 了.
passwd文件里面存放着用户名，什么权限的用户都能查看 cat /etc/passwd，

> 如何提权到root权限，上面介绍了几种方法了
sudo提权、直接运行sudo bash、sudo su root、sudo + python提权、计划任务提权
这些都是普通用户获取权限——最高权限（root权限）的方法
kali虚拟机里面还能直接打开root终端，本质和sudo su root一样
----
接下来将，shadow文件介绍
> Shadow文件用于存储Linux系统中用户的密码信息，又称为"影子文件"，只有root用户可以查看。cat /etc/shadow
用户名：加密密码：最后一次修改时间：最小修改时间间隔：密码有效期：密码需要变更前的警告天数：
<重要的就账号 密码，其他都是一些无关紧要的东西>
密码过期后的宽限时间：账号失效时间：保留字段

-----
特权账号的创建
> 比如说，我要在Linux操作系统（kali虚拟机）里面创建一个账号baili，并且获得最高权限
那么就需要
在/etc/passwd 和 /etc/shadow这两个文件里面添加baili的信息——把用户ID、组ID都改成0——
不建议 把root改成baili，

> PASS WD文件主要是存放账户的文件，任意用户都可以查看，格式如下
用户名：密码：U ID（用户ID）：GID（组ID）：描述性信息：主目录：默认Shell
> 创建用户需要用到的命令是 
首先，root权限下，useradd xxxx （比如：usersdd baili 、 useradd test）
然后，查看cat /etc/passwd ，发现创建成功
////
下面这条命令能直接创建
useradd -p 'openssl passwd-1-salt 'salt' 123456' test -0 -u 0 -g root- G root -s /bin/bash -d /home/test
test是用户名，123456是密码，其余都是模板
查看cat /etc/passwd ，发现创建成功
复杂命令的话，直接提供用户名，剩下的让AI来写

> 创建完成后，名为test，实为root，仅仅是账号密码的区别。因为对于一台电脑而言，用户名ID:组ID=0:0 ，就是root（电脑默认的）
 根据上述原理，说明bali创建后不易被发现和修复，那么就可以把baili作为 再次进入受害者电脑的后门，下次直接用该账号访问就是最高权限，
如果真不小心被发现的话，可以写一个计划任务，就是定时创建该账号，被删除后自动重新创建（除非把计划任务删了）
有的系统，管理员可能设置禁止root登录，可以修改一下，使其允许root登录

> 再次熟悉一下这个命令
ssh baili@192.168.41.136
从你当前的 Kali 机器，以 baili 这个用户的身份，远程连接到 IP 地址为 192.168.41.136 的那台电脑上，并获得一个命令行终端（Shell）。