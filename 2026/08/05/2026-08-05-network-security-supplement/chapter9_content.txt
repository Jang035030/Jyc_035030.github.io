第十四章 漏洞挖掘实战 
1 .Docker ？
> Docker 是一个开源的容器化平台，它能把应用和它依赖的环境（代码、运行环境、配置）打包成一个标准化的单元——容器
> 可以把它理解成轻量级的虚拟机，但它共享宿主机的内核，所以启动速度是秒级的，资源开销比传统虚拟机小很多
> 我在做安全靶场搭建时经常用 Docker
> docker的常见命令



2 .如何判断一个网站是否存在SQL注入漏洞？
1>触发点（哪里可能存在注入）
URL参数：?id=1、?cat=2、?page=3 等
表单输入框：登录、搜索、留言板、注册等任何能输入的地方
HTTP头：Cookie、User-Agent、Referer 等（较少见）
---
2>特征（三种现象）
① 报错特征（最直接）
输入单引号 ' 或 and 1=1 后，页面返回数据库错误信息，包含以下关键字：
SQL syntax
mysql_fetch_array
Unclosed quotation mark
Microsoft OLE DB Provider
② 布尔特征（无报错时）
输入 and 1=1 页面正常，输入 and 1=2 页面空白或显示不同内容，说明你的条件影响了SQL查询结果。
③ 时间特征（无报错无回显时）
输入 sleep(5) 或 and sleep(5)，页面响应延迟了5秒，说明代码被执行。
---
3>一句话总结
有参数 + 输入特殊字符后页面异常（报错/逻辑变化/变慢）→ 可能存在SQL注入。
按严重程度：报错 > 布尔 > 时间（报错注入最严重）。报错型最好利用，时间型最难但最通用（不管有没有报错都能测）
---
工具扫描
Sqlmap、burp suite工具即可扫描

3 .能用来暴力破解的工具
美杜莎 Medusa，kali里面自带的暴力破解工具，能在windows、Linux系统、macOC上运行。协议数量比Hydra要少一些，优点是非常稳定。
九头蛇 Hydra，kali里面自带的暴力破解工具，能在windows、Linux系统、macOC上运行。支持协议广泛（SSH、FTP、HTTP、MySql、RDP等），但是在高并发或网络不稳定时，可能会不稳定甚至崩溃
Burp Suite
Yakit 
Yakit 可以在很多日常渗透测试场景中替代Burp Suite

4 .给你一个登录框，你该怎么去测试？
（这个是挖掘漏洞的时候，最常见的问题，面试也会考）
弱口令（弱密码，像admin password 123456等）
找一下有没有注册账号功能，看一下js接口（）
找漏洞（万能密码，暴力破解，SQl注入）
接着找 有没有上传文件入口（传一个.php木马进去，获取该木马存放的位置，然后用中国蚁剑来控制受害者网站和服务器）

//就是想办法登录进去

> SRC是国内漏洞响应平台，我们挖漏洞的时候，进去网站之后（黑进去），千万不要改密码。（挖漏洞的目的肯定是为了挣钱嘛，帮公司挖漏洞 公司给钱）
> phpMyAdmin 是一个用 PHP 编写的、基于 Web 的 MySQL 数据库管理工具。
简单来说，它让你不用敲 SQL 命令，直接在浏览器里用鼠标点一点，就能管理 MySQL 数据库     挖漏洞/黑网站的时候，重点关注这个。



5 .什么是js接口？
JS接口就是前端JavaScript代码向后端请求数据时，暴露出来的URL地址（API端点）。
----
举个例子：你打开一个登录页面，输入账号密码，点击登录。浏览器并不会直接把你的账号密码"扔"给后端，而是通过一段JavaScript代码，向后端的一个特定地址（比如 /api/login）发送请求。这个 /api/login 就是一个JS接口。
----
🔍 为什么测试登录框要看JS接口？
因为JS接口会泄露很多后端信息，这些信息可以帮你找到潜在的漏洞
---
🛠️ 怎么找JS接口？
在浏览器里按 F12 打开开发者工具：
然后
> 查看源代码：按 Ctrl+U（查看网页源代码），搜索 .js 文件，点进去看。
> Network（网络）面板：刷新页面，查看所有加载的JS文件，逐一翻阅。
> Sources（来源）面板：直接浏览所有前端资源文件，查看JS代码。
> 使用工具：可以用Burp Suite或Yakit的爬虫功能，自动提取所有JS接口。
//这些方法是并列关系，掌握其中任意一种，都有希望 直接找到JS接口

6 . 接口 ？
> 什么是接口？
接口（API，Application Programming Interface） 简单说，就是不同软件/系统之间沟通的“桥梁”或“约定”。
它定义了一个系统如何向外部提供服务或数据

> 接口有哪些类型？
主要关注前两类：
---Web接口 (HTTP/HTTPS API)
这是最主流的形式，前端和服务器之间、不同系统之间都通过它通信
---库/框架接口 (API)
这是给你写代码时用的，比如编程语言或框架提供的内置函数。
---操作系统接口 (系统调用)
这是软件和操作系统内核打交道的“底层通道”，普通开发者一般接触不到。
---硬件接口
这是硬件和硬件/软件之间的连接标准和协议。

> 接口的组成：


7 .Http状态码
HTTP状态码是服务器告诉客户端“请求结果”的三位数字



📍 渗透测试中特别关注的几个
状态码	
关注原因

401	
说明存在认证机制，可以尝试绕过或爆破

403	
说明资源存在但无权访问，可能存在越权漏洞

404	
可以用来目录扫描（看哪些路径存在）

405	
尝试换方法（比如GET改POST）可能绕过限制

500	
说明服务器执行了你的请求但出错了，可能存在SQL注入

302	
登录接口返回302通常表示登录成功，是判断爆破是否成功的标志

429	
说明有限流防护，暴力破解需要加延迟


8 .什么是 爬虫？
> 爬虫，相当于网络渗透测试中，获取网站信息的步骤
> 爬虫是一种“技术”或“程序”，burp suite和Yakit都有爬虫功能
> 爬虫本身只针对网站（Web应用），所以爬虫也叫网络爬虫